{
  "hooks": [
    {
      "event": "Stop",
      "command": "cargo run --manifest-path /path/to/wyrd-diff/Cargo.toml --locked -p wyrd-diff-cli -- hook agent-stop",
      "timeout": 30000
    }
  ]
}
